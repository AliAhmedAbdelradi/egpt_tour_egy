import 'package:awesome_dialog/awesome_dialog.dart';
import 'package:ept_mate/api_manager/api_manager.dart';
import 'package:flutter/material.dart';

class tourismResCat extends StatefulWidget {
  const tourismResCat({super.key});
  static const String routeName="tourismResCat";

  @override
  State<tourismResCat> createState() => _CityResCatState();
}

class _CityResCatState extends State<tourismResCat> {
  @override
  Widget build(BuildContext context) {
    return  Scaffold(

    );
  }
}
